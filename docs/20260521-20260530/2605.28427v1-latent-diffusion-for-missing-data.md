---
title: Latent Diffusion for Missing Data
title_zh: 缺失数据的潜在扩散
authors: "Alberte Heering Estad, Ignacio Peis, Jes Frellsen"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28427v1"
tags: ["query:eeg-latent"]
score: 8.0
evidence: 用于缺失数据插补的潜在扩散模型
tldr: "扩散模型在缺失数据插补中表现出色，但像素空间方法在训练数据高度不完整时退化严重。本文提出两阶段框架：先利用鲁棒VAE从缺失观测中学习紧凑潜在表示，再在潜在空间训练扩散模型。实验表明，潜在扩散在高达50%缺失率下仍保持高质量，而像素空间扩散随缺失率增加性能持续下降。该工作表明潜在空间建模能有效缓解伪影放大，为缺失数据学习提供了更鲁棒的生成先验。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28427v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1182, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28427v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1574, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28427v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1574, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28427v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1578, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28427v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1053, \"height\": 374, \"label\": \"Table\"}]"
motivation: 像素空间扩散在数据高度不完整时性能退化，需要更鲁棒的表示来缓解零插补引起的伪影。
method: 两阶段框架：鲁棒VAE从缺失观测中学习紧凑语义特征，然后扩散模型在潜在空间中训练。
result: "潜在扩散在50%缺失率下保持高质量，像素空间扩散性能随缺失率增加而渐进退化。"
conclusion: 潜在空间建模是缺失数据问题中鲁棒且实用的生成式插补替代方案。
---

## 摘要
扩散模型已成为用于缺失数据插补的强大生成方法，但现有大多数方法直接在数据空间中操作，当训练数据高度不完整时性能会下降。我们研究了将扩散转移到学习到的潜在表示是否能在完全随机缺失（MCAR）损坏下提高鲁棒性。为此，我们提出一个两阶段框架：一个基于鲁棒VAE的插补器首先从不完整观测中学习紧凑的语义特征，然后在此生成的潜在空间中训练扩散模型。在不同训练缺失率下，我们在相同的不完整数据设置下与像素空间扩散模型进行了受控比较。潜在扩散模型保持了高样本质量，并在缺失率高达50%时保持稳定，而像素空间扩散随着缺失率增加逐渐退化。对于下游插补任务，潜在扩散也始终比像素空间扩散表现更好。这些发现表明，潜在空间建模减轻了零插补输入带来的伪影放大，并为不完整数据学习提供了更稳健的生成先验。总体而言，我们的结果支持潜在扩散作为缺失数据问题中像素空间扩散的一个强有力且实用的替代方案。

## Abstract
Diffusion models have emerged as powerful generative approaches for missing-data imputation, yet most existing methods operate directly in data space and degrade when training data are heavily incomplete. We investigate whether shifting diffusion to a learned latent representation improves robustness under missing-completely-at-random (MCAR) corruption. To this end, we propose a two-stage framework: a robust VAE-based imputer first learns compact semantic features from incomplete observations, and a diffusion model is then trained in the resulting latent space. Across training missing rates, we perform a controlled comparison against pixel-space diffusion models under the same incomplete-data setting. The latent diffusion model maintains high sample quality and remains stable up to 50\% missingness, while pixel-space diffusion degrades progressively as missingness increases. For downstream imputation, latent diffusion also achieves consistently better performance than pixel-space diffusion. These findings indicate that latent-space modeling mitigates artifact amplification from zero-imputed inputs and provides a more robust generative prior for incomplete-data learning. Overall, our results support latent diffusion as a strong and practically useful alternative to pixel-space diffusion for missing-data problems.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Latent Diffusion for Missing Data》的详细分析总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现实世界的数据集经常不完整（存在缺失值）。扩散模型在缺失数据插补任务上表现出色，但现有大多数方法直接在像素/数据空间操作。当训练数据中缺失率很高时，这些像素空间扩散模型的性能会显著下降。
- **研究动机**：探索是否可以将扩散过程转移到由变分自编码器（VAE）学习到的、更具语义性的潜在空间，从而提高模型在不完整数据训练下的鲁棒性。
- **整体含义**：本文通过系统对比，证明了在缺失数据场景下，潜在空间扩散模型（LDMiss）相比传统像素空间扩散模型（DDPM）具有显著优势。该发现指出了“语义压缩”是缓解因零值插补引起的伪影放大的关键，为在现实世界（尤其是医疗、金融等领域）的高缺失率数据上应用生成模型提供了更稳健的替代方案。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用两阶段框架。
    1. **第一阶段**：训练一个鲁棒的、能处理缺失数据的β-VAE（变分自编码器）。它从不完整观测中学习数据的紧凑、语义化的潜在表示。
    2. **第二阶段**：在这个学习到的潜在空间中训练一个去噪扩散概率模型（DDPM）。

- **关键技术细节**：
    - **处理缺失数据**：
        - **对于VAE**：在训练时，使用零值（Zero-imputation）填充缺失像素，但仅在观测到的维度上计算重构损失（ELBO重构项），并通过条件独立性假设（假设编码器为对角高斯分布）让缺失维度在潜在空间中被自动“分解”，无需额外处理。
        - **对于像素空间扩散（作为对照）**：采用了两种方法。
            - **MissDiff**：在零值插补后，仅在观测维度上计算得分匹配损失。
            - **DiffPuter**：一种基于期望最大化（EM）的迭代算法，在E步插补缺失值，在M步用插补后的完整数据重新训练扩散模型。
    - **潜在扩散模型（LDMiss）**：
        - **VAE（编码器E-解码器D）**：使用β-VAE，将28x28的图像压缩为2x7x7的潜在表示，压缩因子为8。采用极小的KL权重（β=10⁻⁶）以保证高保真度的重建。
        - **潜在空间扩散**：采用方差保持（Variance-Preserving, VP）的随机微分方程（SDE）来定义前向和反向扩散过程。使用简化的降噪得分匹配目标（Equation 2）在潜在表示 `z` 上训练得分网络 `sθ`。
    - **用于缺失数据插补的自引导（Self-Guidance）方法**：
        - **适用性**：用于LDMiss和像素空间DDPM。
        - **原理**：利用贝叶斯规则，条件得分 `∇xt log pt(xt|x_obs)` 可分解为无条件得分和一个指导项。使用Tweedie公式近似计算观测数据的后验均值估计，进而构建一个指导梯度（Equation 5），将生成过程引向与观测数据一致的方向。
        - **LDMiss的特化**：对于LDMiss，先解码潜在的后验均值估计，然后在像素空间计算与观测数据的差异，再通过链式法则将梯度传回潜在空间进行引导。
        - **关键技巧**：对指导项进行缩放，使其量级与无条件得分项大致相等，以确保在所有时间步上都能提供同等强度的引导。

### 3. 实验设计：数据集、基准与对比方法

- **数据集**：在经典的**MNIST**手写数字数据集上进行实验。
- **缺失机制**：假设数据为**完全随机缺失（MCAR）**。
- **对比方法**：
    - **LDMiss**（本文提出）：使用自引导（Self-Guidance）方法进行插补。
    - **像素空间DDPM**：作为基线。
        - 使用 **MissDiff** 损失进行训练。
        - **插补方式**：对比了两种方式：1) 经典的**替换（Replacement）方法**；2) 自引导（Self-Guidance）方法。
    - **DiffPuter**：一种EM框架下的像素空间扩散模型，旨在处理缺失数据。
    - **Autoencoder**：单独训练的自编码器，用于重建/插补。
- **评价指标**：
    - **样本生成质量**：FID和IS（Inception Score）。
    - **插补质量**：MSE（均方误差）、FID、IS。

### 4. 资源与算力

论文在附录A.4中说明了使用的GPU型号：
- 所有DDPM实验在 **NVIDIA Titan V GPU** 上执行。
- 所有LDM实验在 **NVIDIA GeForce RTX 4070 Ti SUPER GPU** 上执行。
- **未明确说明**：文中未提及GPU的具体数量、训练时长（epoch数有50，但迭代次数未提）以及总体计算消耗的详细信息。

### 5. 实验数量与充分性

- **实验数量**：
    - 训练缺失率：在6种不同水平（0%, 10%, 30%, 50%, 60%, 80%）下训练模型。
    - 每个实验使用 **3个不同的随机种子** 进行重复，并报告指标的平均值和方差。
    - 测试插补性能时，在 **50%的固定测试缺失率** 下进行评估。
    - 提供了定量的图表（图1, 图3）和定性的生成样本可视化（图2, 图4）。
- **充分性与公平性**：
    - **优势**：实验设计较为系统。对缺失率这一关键变量进行了精细的消融研究。使用了多个随机种子来评估稳定性，并报告了方差。定量和定性结果相互印证，结论可靠。
    - **客观性**：对比了同一文献中常用的两种像素空间方法（MissDiff和DiffPuter），并确保两种模型在相似的架构（均为DDPM++类U-Net）和训练设置下进行对比，体现了公平性。
    - **局限**：实验场景仅局限于**MNIST**一个数据集和**MCAR**这一种缺失机制。这限制了结论在更复杂、更现实的缺失场景（如MAR, MNAR）下的泛化能力。

### 6. 论文的主要结论与发现

1.  **鲁棒性更强**：LDMiss在保持生成样本质量方面比像素空间DDPM**更稳定、更鲁棒**。
    - LDMiss在50%的训练缺失率下仍能保持高质量，而DDPM的性能从10%缺失率开始就持续恶化。
2.  **插补性能更优**：在所有训练缺失率下，LDMiss的插补性能（通过MSE, FID, IS衡量）都**始终优于**像素空间DDPM。
3.  **关键原因**：潜在空间扩散通过在压缩的语义特征上操作，有效**过滤了因大量零值插补输入而引入的伪影**，从而避免了这些噪声在像素空间扩散过程中被放大。
4.  **相互验证**：定性结果（生成的图像）与定量指标（FID, IS, MSE）高度一致，进一步证实了LDMiss的优势。

### 7. 优点

- **方法创新**：首次系统地研究了将扩散过程迁移到潜在空间对处理不完整训练数据的影响，提出了一个简洁有效的两阶段框架（LDMiss）。
- **问题定位准确**：清晰地指出了像素空间扩散模型在处理高度不完整数据时的根本问题——零值插补引入的伪影会被放大。
- **实验设计严谨**：进行了全面的消融实验，对缺失率这一核心变量进行了广泛扫描，并通过多次实验保证了结果的统计可靠性。对比方法选择恰当。
- **分析深入**：不仅展示了性能优势，还从原理上（伪影放大、语义压缩）进行了解释，增强了论文的洞察力。
- **可复现性**：提供了详细的实验设置（附录A）和代码链接，有助于其他研究者复现和拓展工作。

### 8. 不足与局限

- **场景单一**：实验仅局限于**MNIST**数据集，其为简单、低分辨率的灰度图。该发现是否能在更复杂、高分辨率的自然图像（如ImageNet）或其它模态数据（如表格数据、时间序列）上成立，尚待验证。
- **缺失机制局限**：只考虑了**MCAR**这一最理想化的缺失机制，未涉及更现实和困难的“随机缺失（MAR）”或“非随机缺失（MNAR）”。在MAR和MNAR下，零值插补的偏差更大，LDMiss的效果可能需要进一步验证。
- **潜在的架构差异**：虽然论文试图在相似的架构下对比（均为DDPM++），但像素空间和潜在空间的得分网络架构参数略有不同（如表1所示），这些差异也可能对最终性能有轻微贡献。作者承认了这一点。
- **退化现象**：即使是LDMiss，在极高缺失率（如60-80%）下，生成和插补质量也会出现退化（图像变得模糊）。这说明该方法并非对所有缺失率都完美，存在鲁棒性的上限。
- **零值插补的假设**：VAE的训练依赖于零值插补，这在很多场景下不是最优选择。使用更先进的条件VAE或处理缺失数据的方法作为第一阶段，可能会进一步提升整体性能。

（完）
