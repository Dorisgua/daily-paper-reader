---
title: Nonparametric Deconvolution and Denoising using Simulation Based Inference
title_zh: 基于模拟推断的非参数解卷积与去噪
authors: "Ritwik Vashistha, Abhra Sarkar, Arya Farahi"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21907v1"
tags: ["query:eeg-latent"]
score: 8.0
evidence: 使用潜在生成模型进行非参数解卷积与去噪，与EEG潜在空间表示相关
tldr: 潜在信号常被测量噪声掩盖，现有方法难以处理复杂生成模型的似然。本文提出基于卷积最大均值差异的无似然框架，通过匹配噪声卷积后的模型分布来学习潜在生成模型，实现非参数密度去卷积和经验贝叶斯去噪。理论将convMMD推广到非参数，证明有限样本界和L2收敛率，恢复经典反问题依赖性。该方法为生成式潜在分布模型下的信号恢复提供了实用且有理论保障的途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21907v1/fig-001.webp\", \"caption\": \"\", \"page\": 9, \"index\": 1, \"width\": 8073, \"height\": 3940}, {\"url\": \"assets/figures/arxiv/2606.21907v1/fig-002.webp\", \"caption\": \"\", \"page\": 9, \"index\": 2, \"width\": 7267, \"height\": 4905}]"
motivation: 潜在信号被噪声掩盖且似然难解，亟需无似然的非参数去卷积和去噪方法。
method: 利用卷积最大均值差异损失，匹配观测分布与噪声卷积模型分布，学习潜在生成模型并用于经验贝叶斯去噪。
result: 理论证明经验筛子极小化器的有限样本界和Sobolev光滑性下的L2收敛率，恢复多项式或对数收敛速率。
conclusion: 为噪声下信号和分布学习提供实用、理论完善的去卷积与去噪框架。
---

## 摘要
潜在信号常被测量噪声掩盖，却编码了复杂系统的基本规律和动力学；同时学习信号及其分布仍是科学推断中的核心挑战。噪声往往不可忽略，而表达性生成模型的似然函数通常难以处理。我们利用卷积最大均值差异（convMMD）损失，提出了一种在加性测量误差下进行非参数密度解卷积和经验贝叶斯去噪的无似然框架。该方法通过将观测数据分布与噪声卷积后的模型分布进行匹配，学习潜在生成模型。这为多元同方差或异方差噪声提供了一个可微的、基于模拟的目标函数，与高斯混合和归一化流等表达性筛类兼容。学习到的密度随后作为经验先验，用于个体潜在值的后验去噪。理论上，我们将convMMD从参数估计扩展到非参数估计，证明了经验筛最小化器的有限样本界以及Sobolev光滑性下的$L_2$收敛速度。这些速度恢复了经典反问题的依赖关系：普通光滑噪声为多项式速度，超光滑噪声为对数速度。我们的方法为生成性潜在分布模型下的解卷积与去噪提供了一种实用且理论严谨的途径。

## Abstract
Latent signals are often obscured by measurement noise, yet encode the underlying laws and dynamics of complex systems; learning both the signals and their distributions remains a central challenge in scientific inference. The noise is often non-negligible, and the likelihoods for expressive generative models are often intractable. We utilize a convolutional maximum mean discrepancy (convMMD) loss and propose a likelihood-free framework for nonparametric density deconvolution and empirical Bayes denoising under additive measurement error. Our method learns a latent generative model by matching the observed data distribution to the noise-convolved model distribution. This yields a differentiable, simulation-based objective for multivariate homoscedastic or heteroscedastic noise, compatible with expressive sieve classes such as Gaussian mixtures and normalizing flows. The learned density then serves as an empirical prior for posterior denoising of individual latent values. Theoretically, we extend convMMD from parametric to nonparametric estimation, proving finite-sample bounds for empirical sieve minimizers and $L_2$ convergence rates under Sobolev smoothness. These rates recover the classical inverse-problem dependence: polynomial for ordinary-smooth and logarithmic for super-smooth noises. Our method provides a practical, theoretically grounded approach to deconvolution and denoising under generative latent distribution models.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在许多科学领域（如天文学），观测数据是受测量噪声污染的潜在信号的代理。忽略测量误差会导致分析偏离真正感兴趣的科学对象。同时，不仅需要恢复潜在信号的**分布**（去卷积），还需要恢复每个观测对应的**个体潜在值**（去噪）。
- **核心挑战**：噪声常不可忽略，且对于表达能力强的生成模型（如归一化流、GAN），其似然函数往往难以计算或不可得。经典的非参数去卷积方法（如傅里叶反演、核估计）在高维和复杂生成模型面前面临扩展性困难。
- **本文意义**：提出一个**无似然的、基于模拟推断的框架**，利用卷积最大均值差异（convMMD）损失，直接从噪声观测中学习潜在信号的生成模型，并用于经验贝叶斯去噪。该方法兼具理论保证（有限样本界、非参数收敛率）和实际灵活性（兼容各类筛类生成模型）。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：当已知噪声分布 $m$ 时，对于候选潜在密度 $q_\theta$，将其与噪声卷积得到 $q_\theta * m$，然后最小化该模型分布与观测数据分布之间的**卷积MMD**（convMMD）：
  \[
  \hat{\theta}_N = \arg\min_{\theta \in \Theta_J} \text{MMD}_k^2\left((p*m)_N, q_\theta * m\right)
  \]
  其中 $(p*m)_N$ 是噪声观测的经验分布。该目标可直接通过蒙特卡洛采样和自动微分优化。
- **关键技术细节**：
  - **筛类框架**：使用参数复杂度递增的筛类 $\mathcal{Q}_J$（如高斯混合模型、归一化流），保证估计的一致性和收敛率。
  - **convMMD**：利用平移不变核，将convMMD转化为潜在RKHS中的MMD，从而获得参数收敛率（$O_p(N^{-1/2})$）。
  - **非参数L2收敛率**：在Sobolev光滑性假设下，进一步证明：普通光滑噪声（多项式衰减）获得多项式收敛速度；超光滑噪声（指数衰减）获得对数收敛速度。
  - **经验贝叶斯去噪**：将学习到的潜在密度 $q_{\hat{\theta}_N}$ 作为经验先验，通过后验均值 $\mathbb{E}_{q_{\hat{\theta}_N}}[X|\tilde{X}]$ 进行去噪。
- **算法流程**：在每步优化中，从潜在模型采样 $Y$，加上噪声采样 $U$ 得到模型噪声样本 $\tilde{Y}=Y+U$，计算convMMD蒙特卡洛估计，使用路径梯度更新参数。

### 3. 实验设计：数据集、基准、对比方法
- **一维收敛率验证**：真实潜在密度为两分量Laplace混合，噪声为异方差。对比普通光滑（Laplace噪声+Laplace核）和超光滑（Gaussian噪声+Gaussian核）两种情况。筛类使用高斯混合模型（GMM）和归一化流（NF）。评估$L_2$误差和潜在MMD。
- **二维去噪（存在离群点）**：使用Three Moons、Circle、Checkerboard三个拓扑结构化数据集，并加入3%离群点。对比方法：NPEB（非参数经验贝叶斯）、XDGMM（极值去卷积）、Oracle（基于大样本KDE）。评估指标为均方误差（MSE）和Wasserstein-1距离。
- **高维可扩展性**：使用低维Tangled Ribbon流形嵌入到 $D\in\{3,7,11,15\}$ 空间中，加入异方差高斯噪声。对比方法：XDGMM、NPEB、deconv（归一化流+变分推断）。测试三种噪声知识条件：完全已知、同方差误指定、异方差误指定。评估切片Wasserstein距离（SWD）。
- **高维图像去噪（MNIST）**：$D=784$，无标签噪声图像学习先验，然后经验贝叶斯去噪。噪声类型：加性白高斯噪声（AWGN）和AR(1)空间相关噪声。对比方法：Noise2Self（自监督）、SURE（无监督）、BUIFD（有监督上界）。评估SSIM和PSNR。
- **核带宽敏感性分析**：在三个2D数据集上改变带宽缩放因子，观察MSE和W1变化。

### 4. 资源与算力
论文未明确说明使用的GPU型号、数量或具体训练时长。仅提到使用AdamW优化器和余弦学习率衰减等常规设置。实验规模中等（最大样本量32000，图像维度784），推测可在单卡GPU（如V100或A100）上完成，但缺乏具体报告。

### 5. 实验数量与充分性
- **覆盖范围**：涵盖一维收敛率、二维有离群点去噪、多维流形恢复、高维图像去噪，以及核敏感性分析，场景多样。
- **重复性**：大部分实验报告了多次独立运行的平均值和标准差（如5次、20次）。
- **对比公平性**：对比了经典基准（NPEB、XDGMM）和专用图像去噪方法（Noise2Self、SURE、BUIFD）。但在图像实验中，作者强调该实验仅为展示可扩展性，而非与图像去噪社区竞争。
- **充分性**：理论验证和消融实验（核带宽）较充分。但缺少对超参数（如MMD核类型、模型架构大小）的系统消融，且未在真实高维科学数据（如天文观测）上评估，限制了实际应用的说服力。

### 6. 论文的主要结论与发现
- convMMD框架成功实现非参数去卷积和去噪，理论收敛率与经典反问题结果一致。
- 在普通光滑噪声下获得多项式L2收敛率，在超光滑噪声下获得对数收敛率；潜在RKHS中的MMD收敛始终保持参数速率 $O_p(N^{-1/2})$。
- 在合成拓扑数据集上，convMMD（尤其搭配归一化流）在MSE和W1上优于NPEB和XDGMM，且对核带宽和噪声误指定鲁棒。
- 在高维流形恢复中，convMMD在 $D=11,15$ 时显著优于所有基线。
- 在MNIST图像去噪中，convMMD对空间相关噪声表现出稳定性，而依赖像素独立性假设的方法（Noise2Self）性能崩溃。

### 7. 优点
- **理论严谨**：给出有限样本oracle不等式、参数/非参数收敛率，并将理论推广到经验贝叶斯去噪。
- **方法灵活**：兼容高斯混合、归一化流、GAN等任意可采样生成模型，无需似然函数。
- **无似然**：通过匹配噪声卷积后的分布避免求解反问题，使优化稳定。
- **统一框架**：同一次学习支持密度去卷积和个体去噪。
- **鲁棒性**：对核带宽、噪声误指定、离群点均有良好可容忍性。

### 8. 不足与局限
- **理论假设较强**：需要噪声分布已知、Sobolev光滑性、紧支撑等条件；优化假设达到全局极小值（非凸问题）。
- **高维图像实验为概念验证**：未与最先进图像去噪方法全面比较，也未使用真实科学数据。
- **参数选择依赖**：MMD核带宽、筛类架构的选择仍依赖启发式，理论指导不足。
- **可扩展性限制**：虽然MNIST维度784可行，但更高维（如自然图像）可能仍受限于MMD的计算代价和模型容量。
- **未讨论计算复杂度**：大规模蒙特卡洛采样和梯度计算的开销未被分析。
- **噪声误指定鲁棒性仅部分验证**：仅测试了同方差方差误指定和异方差有偏估计，未测试分布族误指定（如实际噪声非高斯）。

（完）
